package Practise;

public class GreaterOrNotorEqual {
    // User function Template for Java
    class Solution {
        public static String compareNM(int n, int m) {
            // code here
            return (n < m) ? "lesser" : (n > m) ? "greater" : "equal";
        }
    }
}
